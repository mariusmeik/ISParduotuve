@model IEnumerable<ProjectParduotuve.Models.ProognozuojamaPreke>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.reikalingasKiekis)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pirkimoProognoze)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.reikalingasKiekis)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pirkimoProognoze)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
