@model IEnumerable<ProjectParduotuve.Models.Vieta>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pavadinimas)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adresas)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Paskirtis)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pavadinimas)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Adresas)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Paskirtis)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_Vieta }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_Vieta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_Vieta })
        </td>
    </tr>
}

</table>
